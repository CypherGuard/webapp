version: '3.7'
services:
  webapp:
    container_name: webapp_container
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:3000"
    expose:
      - "8080"
    volumes:
      - ./:/webapp
      - /node_modules